# ---- Local development example ----
SPRING_PROFILES_ACTIVE=dev
PORT=8080

# Database (dev)
DB_URL=jdbc:mysql://localhost:3306/zoo_db?useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=

# JWT (dev/testing only)
JWT_SECRET=change-me-dev
JWT_EXP_MINUTES=60

# Prod için gerçek değerler asla commit edilmez.
# SPRING_PROFILES_ACTIVE=prod ve tüm secret'lar ENV/secrets store üzerinden verilir.
