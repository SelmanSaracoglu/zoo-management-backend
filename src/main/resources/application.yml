spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zoo_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Root1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  jackson:
    serialization:
      indent_output: true

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:abcdefghijklmnopqrstuvwxyz012345abcdefghijklmnopqrstuvwxyz012345}
    expiration-ms: 3600000

